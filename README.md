Data Preprocessing

Minimal preprocessing utilities using NumPy/SciPy/pandas. Demonstrates feature normalization and simple STFT.

Run:

python -m pip install -r requirements.txt
python preprocess.py sample.csv out.npz
